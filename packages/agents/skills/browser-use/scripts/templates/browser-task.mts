/**
 * Placeholder template for one-off low-level browser tasks.
 *
 * This file should eventually become the default starting point for scripts
 * that use `connect(...)` and `chrome.call(...)` directly.
 *
 * What this template should eventually include:
 * - CLI argument parsing with `--help`
 * - deterministic logging prefix
 * - bounded timeouts
 * - connect helper usage
 * - optional tab/window creation
 * - optional markdown extraction via `chrome.getPageMarkdown(...)`
 * - optional screenshot/download helpers
 * - cleanup and explicit `process.exit(...)`
 *
 * What we need to learn first:
 * - what the minimum reusable CLI structure should be
 * - which setup/cleanup steps are universal enough to template
 * - which output file conventions should be standard even for one-off tasks
 */

export {};

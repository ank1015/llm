/**
 * Placeholder template for interactive `Window`-based browser tasks.
 *
 * This file should eventually become the default starting point for flows that
 * use `Window.observe()` and action helpers such as click/type/select/scroll.
 *
 * What this template should eventually include:
 * - CLI argument parsing with `--help`
 * - `Window` setup
 * - initial navigation
 * - observe-before-act workflow
 * - action result logging
 * - fallback hook when `Window` actions are insufficient
 * - cleanup and explicit `process.exit(...)`
 *
 * What we need to learn first:
 * - what the smallest reliable `Window` workflow looks like
 * - what diagnostics are needed around observe/action cycles
 * - when the template should suggest falling back to low-level APIs
 */

export {};

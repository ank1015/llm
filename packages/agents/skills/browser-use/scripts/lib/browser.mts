/**
 * Placeholder library for shared browser helpers.
 *
 * This file should eventually hold the helpers that many browser scripts need,
 * such as:
 * - connect wrapper
 * - create working window/tab helpers
 * - wait-for-load helpers
 * - focus stabilization helpers
 * - navigation helpers
 *
 * We should not fill this file until those helpers are validated through
 * multiple real scripts; otherwise we will freeze in brittle abstractions too
 * early.
 */

export {};

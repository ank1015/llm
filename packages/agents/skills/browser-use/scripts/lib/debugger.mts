/**
 * Placeholder library for shared debugger and CDP helpers.
 *
 * This file should eventually hold:
 * - attach/detach guards
 * - `Runtime.evaluate` wrappers
 * - network capture helpers
 * - screenshot or layout-metric helpers if they prove reusable
 *
 * We should only fill this after repeated experiments confirm which debugger
 * patterns are universal and which are task-specific.
 */

export {};

# SDK Docs

This folder contains package-local documentation for `@ank1015/llm-sdk`.

- [Adapter Boundaries](./adapters.md) explains what stays in `sdk`, what belongs in `sdk-adapters`, and how credential/session contracts are expected to behave.
- [Testing and Ownership](./testing.md) explains which tests belong in this package, which moved to `sdk-adapters`, and how to run the remaining checks.
